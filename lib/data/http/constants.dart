// Default settings from Firefox, with the values set to be smaller
const Duration kDefaultConnectTimeout = Duration(milliseconds: 30000);
const Duration kDefaultSendTimeout = Duration(milliseconds: 3000);
const Duration kDefaultReceiveTimeout = Duration(milliseconds: 20000);

// Base API URL
const String kBaseUrl = "https://swapi.dev/api/";
